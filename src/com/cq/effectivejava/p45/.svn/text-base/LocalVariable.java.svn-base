/**2012-6-24**/

package com.cq.effectivejava.p45;

/**
 * 将局部变量的作用域最小化 1，在第一次使用局部变量的地方声明他 2局部变量不同于实例变量，一定需要被初始化
 * 
 * @author 陈强
 * @since 1.0
 * @version 2012-6-24 陈强新建
 */
public class LocalVariable {
    
    public static void main(String[] args) {
        // 使用j减少多args长度的冗余计算
        for (int i = 0, j = args.length; i < j; i++) {
            
        }
    }
}
